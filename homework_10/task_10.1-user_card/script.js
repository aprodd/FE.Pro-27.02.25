const userInfo = {
    name: 'Adam',
    age: 30,
    city: 'Boston',
    sport: 'boxing'
};

let arrKeys = Object.keys(userInfo);
let arrValues = Object.values(userInfo);

console.log(arrKeys);
console.log (arrValues);
